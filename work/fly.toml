app = 'hj-work-planner'
primary_region = 'nrt'

[build]

[http_service]
  internal_port = 8001
  force_https = true
  auto_stop_machines = 'suspend'
  auto_start_machines = true
  min_machines_running = 0

[mounts]
  source = 'work_data'
  destination = '/app/data'
